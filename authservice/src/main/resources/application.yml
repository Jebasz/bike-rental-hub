spring:
  application:
    name: authservice
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: portfolio123@
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format-sql: true
